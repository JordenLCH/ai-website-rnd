{
  "name": "@blackdash/site-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "site-mcp": "./bin/site-mcp.mjs"
  },
  "scripts": {
    "start": "node --import tsx src/server.ts",
    "http": "node --import tsx src/http.ts",
    "smoke": "node --import tsx src/smoke.ts",
    "build:app": "npm --prefix app install --silent && npm --prefix app run build",
    "prove": "node --import tsx src/prove.ts"
  },
  "dependencies": {
    "@blackdash/renderer": "file:../renderer",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1",
    "@blackdash/platform": "file:../platform"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}